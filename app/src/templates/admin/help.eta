<% layout("/layouts/admin-base", { title: "Help", user: it.user, adminBase: it.adminBase, csrfToken: it.csrfToken }) %>
<section class="page page-help">
  <header class="page-head">
    <h1>Help &amp; documentation</h1>
    <p>Everything you need to run your portfolio site. Three things to know first:</p>
    <ol class="lead-points">
      <li><strong>The left sidebar</strong> is the only menu — each item manages one kind of content or setting.</li>
      <li><strong>Changes save instantly to the database</strong> — but the live site only updates when you click <strong>Publish</strong> (bottom-left of the sidebar).</li>
      <li><strong>Hover any field with a small <code>?</code></strong> for a quick explanation.</li>
    </ol>
  </header>

  <nav class="help-toc" aria-label="On this page">
    <strong>On this page:</strong>
    <a href="#getting-started">Getting started</a> ·
    <a href="#publish">The Publish flow</a> ·
    <a href="#sections">Sections</a> ·
    <a href="#editor">The rich-text editor</a> ·
    <a href="#faq">FAQ</a> ·
    <a href="#troubleshooting">Troubleshooting</a> ·
    <a href="#backups">Backups</a>
  </nav>

  <article class="help-article">

  <h2 id="getting-started">Getting started</h2>
  <p>Your first session usually goes:</p>
  <ol>
    <li>Edit <strong>Settings</strong> to set your site title, brand, social links, and contact email.</li>
    <li>Pick a <strong>Theme</strong> — clone &amp; edit a preset if you want custom colors.</li>
    <li>Pick <strong>Fonts</strong> — one sans + one mono.</li>
    <li>Edit the four system <strong>Pages</strong> (about, now, contact, 404). Add custom pages with <strong>+ New page</strong>.</li>
    <li>Add a <strong>Work</strong> entry or a <strong>Writing</strong> post.</li>
    <li>Adjust the <strong>Menu</strong> — drag-ish reorder with ↑↓, add a Résumé / external link, pick a hover style.</li>
    <li>Click <strong>Publish</strong>. The public site regenerates from scratch (typically under 2 seconds).</li>
  </ol>

  <h2 id="publish">How Publish works</h2>
  <p>The public site is a directory of plain HTML files in <code>public/</code>. The admin app never serves them dynamically — Apache (or Tailscale Serve, or any static host) hands the files out as-is. So <em>nothing</em> you change in the admin appears live until Publish regenerates those files.</p>
  <p>Publish is atomic — it writes the whole site into <code>public.next/</code>, then renames it onto <code>public/</code>. Visitors never see a half-rendered site.</p>
  <p>When in doubt: <strong>save, then Publish.</strong></p>

  <h2 id="sections">Sidebar sections</h2>

  <h3 id="dashboard">Dashboard</h3>
  <p>Tile shortcuts plus the Publish button (also always in the sidebar footer).</p>

  <h3 id="pages">Pages</h3>
  <p><em>Path: <code>/admin/pages/</code></em></p>
  <p>Standalone pages at the root of the site — <code>/about.html</code>, <code>/now.html</code>, <code>/contact.html</code>, <code>/404.html</code>, plus any custom slug you create.</p>
  <ul>
    <li><strong>System pages</strong> (about, now, contact, 404) can be edited but never deleted.</li>
    <li><strong>+ New page</strong> creates a fresh page at <code>/your-slug.html</code>. Slug must be lowercase kebab-case.</li>
    <li>Some slugs are blocked because they collide with generated paths: <code>admin</code>, <code>assets</code>, <code>index</code>, <code>work</code>, <code>writing</code>, <code>sitemap</code>, <code>robots</code>, <code>feed</code>, <code>favicon</code>, <code>uploads</code>, <code>static</code>.</li>
  </ul>

  <h3 id="work">Work</h3>
  <p><em>Path: <code>/admin/work/</code></em></p>
  <p>Structured project cards. Top 3 (by sort order, non-disabled) appear on the home page. All non-disabled show on <code>/work/</code>. Each has a slug → <code>/work/&#123;slug&#125;.html</code>.</p>
  <p>Fields: kicker (small text above the title on cards), eyebrow (small text above the H1 on the detail page), lede (one-sentence summary), article meta (small line under the lede), tags (comma-separated), cover image (from the media library or a legacy filename), body (rich text).</p>
  <p><strong>Coming-soon mode:</strong> turn on <em>Disabled</em>. Renders a non-clickable card on <code>/work/</code>, no detail page generated.</p>

  <h3 id="writing">Writing</h3>
  <p><em>Path: <code>/admin/writing/</code></em></p>
  <p>Long-form posts at <code>/writing/&#123;slug&#125;.html</code>. The two most recently published also appear on the home page. Toggle <em>Published</em> off to keep something out of the public site (drafts).</p>

  <h3 id="media">Media</h3>
  <p><em>Path: <code>/admin/media/</code></em></p>
  <p>Two upload forms:</p>
  <ul>
    <li><strong>Images</strong> — JPEG / PNG / WebP, ≤ 8 MB. Re-encoded to strip EXIF, compressed. WebP variant generated automatically on publish; the public site uses <code>&lt;picture&gt;</code> with a JPEG fallback.</li>
    <li><strong>Files</strong> — PDF / ZIP / MP3 / MP4, ≤ 32 MB. Stored as-is. Land at <code>/assets/files/</code> on publish.</li>
  </ul>
  <p>Upload validation checks the file's actual content (magic bytes), not just the extension. Renaming <code>script.txt</code> to <code>script.jpg</code> won't fool it.</p>

  <h3 id="themes">Themes</h3>
  <p><em>Path: <code>/admin/themes/</code></em></p>
  <p>5 shipped presets — Cream Light + Indigo Dark, Paper Light + Ink Dark, Solarized, Nord, Warm Sepia. Each defines 10 color tokens in light + dark variants.</p>
  <p><strong>Presets are read-only.</strong> To customize: <em>Clone</em> beside the preset, then <em>Edit</em> the copy. Each color is a picker plus hex input, with a live iframe preview.</p>
  <p><strong>Activate</strong> sets the active theme in the database. <strong>Publish</strong> regenerates <code>tokens.css</code> with the new colors.</p>

  <h3 id="menu">Menu</h3>
  <p><em>Path: <code>/admin/menu/</code></em></p>
  <p>Three tabs. <strong>Items</strong> is the default.</p>
  <ul>
    <li><strong>Items</strong> — add / edit / reorder / hide / delete nav links. Set a parent to nest as a submenu child (one level deep). Toggle ★ <em>featured</em> for the highlight treatment. <em>Visible</em> off hides without deleting.</li>
    <li><strong>Hover style</strong> — pick one of 6 mouseover effects (underline slide, pill fill, spotlight glow, magnetic snap, bracket frame, background slide). Each tile renders a live preview.</li>
    <li><strong>Featured style</strong> — pick one of 5 treatments that apply to ★ items: star prefix, solid pill, outline pill, thick underline, corner badge.</li>
  </ul>
  <p>Both styles are independent and combine cleanly — e.g. bracket-frame hover + solid-pill featured.</p>

  <h3 id="fonts">Fonts</h3>
  <p><em>Path: <code>/admin/fonts/</code></em></p>
  <p>Pick one sans family + one mono family. 6 bundled OFL-licensed families ship in the repo (Geist, Inter, IBM Plex Sans + the three mono variants). Plus 2 "System" stacks that use the OS-native fonts without any download.</p>
  <p>Live preview shows each face. WOFF2 files for the active fonts are copied into <code>public/assets/fonts/</code> on publish — no external CDN.</p>

  <h3 id="settings">Settings</h3>
  <p><em>Path: <code>/admin/settings/</code></em></p>
  <p><strong>Site identity:</strong> title (in <code>&lt;title&gt;</code>), brand (header), tagline (header), home page lede.</p>
  <p><strong>Contact + social:</strong> email, public base URL (used for absolute sitemap entries and og:image), GitHub / Hugging Face / LinkedIn / calendar URLs.</p>

  <h2 id="editor">The rich-text editor</h2>
  <p>Available wherever you see a body field (Pages, Work, Writing). Toolbar from left to right:</p>
  <ul>
    <li><strong>B / I / U</strong> — bold, italic, underline.</li>
    <li><strong>H2 / H3 / P</strong> — headings and paragraphs.</li>
    <li><strong>•&nbsp;list / 1.&nbsp;list / “&nbsp;” / &lt;/&gt;</strong> — bulleted, numbered, blockquote, inline code.</li>
    <li><strong>Link</strong> — prompts for an http(s) or mailto URL.</li>
    <li><strong>Insert ▾</strong> — drops down to four insertions:
      <ul>
        <li><strong>Image</strong> — picks from your image library, inserts an <code>&lt;img&gt;</code>.</li>
        <li><strong>YouTube embed</strong> — paste any YouTube URL or 11-char video ID. Inserts a privacy-friendly <code>youtube-nocookie</code> iframe.</li>
        <li><strong>Code block</strong> — inserts <code>&lt;pre&gt;&lt;code&gt;...&lt;/code&gt;&lt;/pre&gt;</code> for you to type into.</li>
        <li><strong>File download</strong> — picks from your file library, inserts a download link.</li>
      </ul>
    </li>
    <li><strong>Clear</strong> — strips formatting from the current selection.</li>
    <li><strong>Source</strong> (right side) — toggles between visual and raw HTML. Useful for fine-tuning or pasting your own markup.</li>
  </ul>
  <p>Everything saved is run through server-side sanitization. Disallowed tags (script, iframe from non-YouTube hosts, form, object, embed), event handlers (onclick, etc.), and inline styles are removed.</p>

  <h2 id="faq">FAQ</h2>
  <dl class="help-faq">
    <dt>How do I make my changes appear on the live site?</dt>
    <dd>Click <strong>Publish</strong> in the bottom-left of the sidebar.</dd>

    <dt>How do I add a new page?</dt>
    <dd><strong>Pages → + New page</strong>. Pick a slug (lowercase kebab-case), title, body. Save. Publish.</dd>

    <dt>How do I add the new page to the navigation menu?</dt>
    <dd><strong>Menu → Items → + Add</strong>. Set the href to <code>your-slug.html</code>. Save. Publish.</dd>

    <dt>How do I embed a YouTube video in a page?</dt>
    <dd>In the page body editor, click <strong>Insert ▾ → YouTube embed</strong> and paste the URL.</dd>

    <dt>How do I let visitors download a PDF?</dt>
    <dd><strong>Media → Upload file</strong>, then in any editor <strong>Insert ▾ → File download</strong> and pick it.</dd>

    <dt>What's a "slug"?</dt>
    <dd>The URL-safe version of a title. "About me" becomes <code>about-me</code>, and the page lives at <code>/about-me.html</code>.</dd>

    <dt>Can I customize a preset theme?</dt>
    <dd>Yes — <strong>Themes → Clone</strong> beside it, then edit the copy. Presets themselves are read-only.</dd>

    <dt>How do I switch light / dark mode?</dt>
    <dd>Sun / moon button at the bottom of the sidebar. Affects both the admin and (on next page load) the public site, since they share the preference.</dd>

    <dt>Can I undo a change?</dt>
    <dd>Yes — restore from the daily backup at <code>/var/backups/portfolio-cms/portfolio-YYYY-MM-DD.db</code>. See <a href="#backups">Backups</a> below.</dd>

    <dt>What's the difference between Pages and Work?</dt>
    <dd>Pages are standalone (about, now, contact, custom). Work entries are structured project cards with covers and tags shown on <code>/work/</code> and the home page.</dd>

    <dt>Why is the Delete button missing on some pages?</dt>
    <dd>System pages (about, now, contact, 404) can't be deleted, only edited. The site depends on them.</dd>

    <dt>How do I see / edit the raw HTML the editor produced?</dt>
    <dd>Click <strong>Source</strong> at the right of the editor toolbar. Click <strong>Visual</strong> to switch back.</dd>
  </dl>

  <h2 id="troubleshooting">Troubleshooting</h2>
  <table class="help-table">
    <thead><tr><th>Symptom</th><th>Likely cause</th><th>Fix</th></tr></thead>
    <tbody>
      <tr><td>My change doesn't show on the public site</td><td>You haven't published yet</td><td>Click <strong>Publish</strong> in the sidebar</td></tr>
      <tr><td>Theme Activate did nothing visible</td><td>Activate flips the database flag; Publish regenerates the CSS</td><td>Publish</td></tr>
      <tr><td>Image rejected with "Unsupported or unrecognised format"</td><td>Not actually a JPEG / PNG / WebP — magic bytes don't match the extension</td><td>Re-export from your image tool</td></tr>
      <tr><td>File rejected with "Unsupported file type"</td><td>Not PDF / ZIP / MP3 / MP4</td><td>Convert to one of those</td></tr>
      <tr><td>Upload too large</td><td>&gt; 8 MB image or &gt; 32 MB file</td><td>Resize or compress</td></tr>
      <tr><td>Theme Edit button missing</td><td>It's a preset (read-only)</td><td><strong>Clone</strong> first, then edit the copy</td></tr>
      <tr><td>Slug rejected as "collides with a generated path"</td><td>You picked a reserved word (admin, work, writing, sitemap, robots, favicon, etc.)</td><td>Pick something else</td></tr>
      <tr><td>Menu item missing from the public site</td><td>Visible is off, or you haven't published</td><td>Toggle visible / Publish</td></tr>
      <tr><td>YouTube embed shows a blank frame</td><td>URL wasn't a valid YouTube link</td><td>Use a real watch URL or 11-char ID</td></tr>
      <tr><td>Hard-refresh needed to see changes</td><td>Browser cached old CSS/HTML</td><td>Ctrl+Shift+R (Cmd+Shift+R on Mac)</td></tr>
      <tr><td>Login fails 5+ times</td><td>Rate-limited (5 attempts / 15 min)</td><td>Wait 15 minutes, or run <code>npm run reset-password</code> on the server</td></tr>
      <tr><td>Publish button shows 409</td><td>A publish is already running</td><td>Wait a few seconds, retry</td></tr>
    </tbody>
  </table>

  <h2 id="backups">Backups &amp; restore</h2>
  <p>A systemd timer (<code>portfolio-cms-backup.timer</code>) runs daily at 03:30 UTC. It uses SQLite's online-backup API (no app downtime) and rsyncs the uploads dir to <code>/var/backups/portfolio-cms/</code>. The last 14 snapshots are retained.</p>
  <p><strong>Manual backup:</strong></p>
  <pre><code>sudo systemctl start portfolio-cms-backup.service
ls /var/backups/portfolio-cms/</code></pre>
  <p><strong>Restore from a snapshot:</strong></p>
  <pre><code>sudo systemctl stop portfolio-cms
cp /var/backups/portfolio-cms/portfolio-YYYY-MM-DD.db data/portfolio.db
rsync -a --delete /var/backups/portfolio-cms/uploads/ uploads/
sudo systemctl start portfolio-cms
npm run publish</code></pre>

  </article>
</section>

<style>
.page-help { max-width: 820px; }
.lead-points { margin: 12px 0 0; padding-left: 22px; }
.lead-points li { margin-bottom: 6px; font-size: 14px; color: var(--admin-fg-muted); }
.lead-points strong { color: var(--admin-fg); }
.help-toc { margin: 0 0 32px; padding: 12px 16px; background: var(--admin-bg-elevated); border: 1px solid var(--admin-border); border-radius: var(--admin-radius); font-size: 13px; color: var(--admin-fg-muted); line-height: 1.8; }
.help-toc a { color: var(--admin-accent); }
.help-article h2 { margin: 36px 0 12px; font-size: 22px; color: var(--admin-fg); border-bottom: 1px solid var(--admin-border); padding-bottom: 6px; }
.help-article h3 { margin: 24px 0 8px; font-size: 16px; color: var(--admin-fg); }
.help-article p, .help-article li, .help-article dd { color: var(--admin-fg-muted); line-height: 1.6; }
.help-article p { margin: 0 0 12px; }
.help-article ul, .help-article ol { padding-left: 22px; margin: 0 0 16px; }
.help-article li { margin-bottom: 4px; }
.help-article code { background: var(--admin-bg); padding: 1px 5px; border-radius: 3px; font-size: 0.9em; }
.help-article pre { background: var(--admin-bg); padding: 12px 14px; border-radius: var(--admin-radius-sm); overflow-x: auto; font-size: 12px; line-height: 1.5; border: 1px solid var(--admin-border); }
.help-article pre code { background: transparent; padding: 0; }
.help-article em { color: var(--admin-fg-muted); }
.help-article a { color: var(--admin-accent); }

.help-faq dt { font-weight: 600; color: var(--admin-fg); margin-top: 14px; font-size: 14px; }
.help-faq dd { margin: 4px 0 0; padding-left: 0; }

.help-table { width: 100%; border-collapse: collapse; margin: 12px 0 24px; font-size: 13px; }
.help-table th { background: var(--admin-bg); padding: 8px 10px; text-align: left; border-bottom: 1px solid var(--admin-border); color: var(--admin-fg-muted); text-transform: uppercase; letter-spacing: .04em; font-size: 11px; font-weight: 500; }
.help-table td { padding: 8px 10px; border-bottom: 1px solid var(--admin-border); vertical-align: top; }
.help-table td:nth-child(3) { color: var(--admin-fg); }
</style>
