{
  "name": "staticportfolio-cms",
  "version": "0.1.0",
  "description": "SQLite-backed CMS that publishes a static portfolio site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "dev": "node --env-file=.env --watch app/src/server.js",
    "start": "node --env-file=.env app/src/server.js",
    "test": "bash -c 'shopt -s globstar; node --test --test-reporter=spec --test-concurrency=1 app/src/**/*.test.js'",
    "create-admin": "node --env-file=.env scripts/create-admin.js",
    "reset-password": "node --env-file=.env scripts/reset-password.js",
    "seed": "node --env-file=.env seed/seed-from-static.js",
    "publish": "node --env-file=.env scripts/publish.js",
    "backup": "node --env-file=.env scripts/backup.js"
  },
  "dependencies": {
    "@fastify/cookie": "^9.3.1",
    "@fastify/csrf-protection": "^6.4.1",
    "@fastify/multipart": "^8.3.0",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/session": "^10.7.0",
    "@fastify/static": "^7.0.4",
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^11.5.0",
    "eta": "^3.5.0",
    "fastify": "^4.28.1",
    "node-html-parser": "^6.1.13",
    "sanitize-html": "^2.13.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {}
}
